generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}
model Customer {
  id         Int     @id @default(autoincrement())
  name       String
  email      String  @unique
  orderId    String
  product    String
  delivered  Boolean
  used       Boolean
  refundDays Int
}